# AI 个人成长计划 — 数据库与接口文档

本文档描述 **ai_gp** 工程当前数据库模型（Flyway **V1–V3**）与需求文档中的 **REST 接口规划**，便于开发与联调对齐。  
实现状态：数据库已落地；**HTTP API 尚未实现**（未引入 `spring-boot-starter-web`，无 Controller）。

---

## 1. 文档约定

| 项 | 说明 |
|:---|:---|
| 数据库 | MySQL 8.x，字符集 `utf8mb4` |
| 命名 | 表/字段：蛇形（`app_user`）；JSON 与接口：驼峰（`weeklyHours`） |
| 迁移 | `src/main/resources/db/migration/V{1,2,3}__*.sql` |
| 版本前缀 | 对外 API 统一 `/v1/`（PRD） |

---

## 2. 数据库总览

| 分组 | 表名 | 迁移 | 说明 |
|:---|:---|:---|:---|
| 核心业务 | `app_user` | V1 | 微信端用户 |
| | `goal` | V1 | 目标 |
| | `plan` | V1 | 计划版本 |
| | `task` | V1 | 排期任务 |
| AI | `ai_invoke_log` | V2 | AI 调用审计 |
| | `ai_prompt` | V2 | Prompt 模板与版本 |
| | `ai_feedback` | V2 | AI 反馈记录 |
| | `ai_model_config` | V2 | 模型参数配置 |
| 后台 | `admin_user` | V3 | B 端管理员 |
| | `goal_decomposition_review` | V3 | 拆解质量评审 |
| | `user_admin_tag` | V3 | 用户异常标注 |
| | `user_subscription_pref` | V3 | 订阅消息开关 |

### 2.1 关系示意（简化）

```
app_user ──┬── goal ──┬── plan ─── task
           │          │
           │          └── (goal_id 冗余于 task 便于查询)
           ├── ai_invoke_log / ai_feedback / user_subscription_pref / user_admin_tag
           └── (admin 侧独立) admin_user ──┬── goal_decomposition_review
                                           └── user_admin_tag.created_by
```

---

## 3. 表结构明细

### 3.1 `app_user`（V1）

| 列名 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| user_id | BIGINT | PK, AI | 用户主键 |
| openid | VARCHAR(64) | NOT NULL, UK | 微信 openid |
| unionid | VARCHAR(64) | | 微信 unionid |
| nickname | VARCHAR(50) | | 昵称 |
| avatar_url | VARCHAR(500) | | 头像 URL |
| weekly_hours | INT | | 每周可用小时 1–40 |
| current_career | VARCHAR(100) | | 当前职业 |
| interest_domains | JSON | | 兴趣领域数组 |
| last_active_at | DATETIME(3) | | 最近活跃（后台列表） |
| profile_completed | TINYINT(1) | NOT NULL, DEFAULT 0 | 是否完成 onboarding |
| created_at | DATETIME(3) | NOT NULL | |
| updated_at | DATETIME(3) | NOT NULL | |

**索引**：`uk_app_user_openid`，`idx_app_user_last_active`。

---

### 3.2 `goal`（V1）

| 列名 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| goal_id | BIGINT | PK, AI | |
| user_id | BIGINT | NOT NULL, FK→app_user | |
| title | VARCHAR(200) | NOT NULL | |
| description | VARCHAR(2000) | | |
| domain | VARCHAR(20) | | 领域枚举字符串 |
| priority | VARCHAR(10) | NOT NULL, DEFAULT 'MEDIUM' | HIGH/MEDIUM/LOW |
| deadline | DATE | | |
| status | VARCHAR(20) | NOT NULL | 见 §5 目标状态 |
| milestones_json | JSON | | AI 拆解里程碑 |
| created_at / updated_at | DATETIME(3) | NOT NULL | |

**索引**：`idx_goal_user_status`，`idx_goal_user_updated`。

---

### 3.3 `plan`（V1）

| 列名 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| plan_id | BIGINT | PK, AI | |
| goal_id | BIGINT | NOT NULL, FK→goal | |
| version | INT | NOT NULL | 版本号递增 |
| status | VARCHAR(10) | NOT NULL | ACTIVE / DEPRECATED |
| trigger_reason | VARCHAR(30) | | 触发原因枚举 |
| generated_at | DATETIME(3) | | |
| ai_prompt_version | VARCHAR(50) | | |
| created_at / updated_at | DATETIME(3) | NOT NULL | |

---

### 3.4 `task`（V1）

| 列名 | 类型 | 约束 | 说明 |
|:---|:---|:---|:---|
| task_id | BIGINT | PK, AI | |
| plan_id | BIGINT | NOT NULL, FK→plan | |
| goal_id | BIGINT | NOT NULL, FK→goal | |
| user_id | BIGINT | NOT NULL, FK→app_user | |
| title | VARCHAR(200) | NOT NULL | |
| description | VARCHAR(1000) | | |
| scheduled_date | DATE | | 计划日期 |
| estimated_minutes | INT | | |
| actual_minutes | INT | | 实际上报 |
| quality_score | INT | | 自评 1–5 |
| status | VARCHAR(10) | NOT NULL | PENDING/COMPLETED/SKIPPED |
| skip_reason | VARCHAR(500) | | |
| completed_at | DATETIME(3) | | |
| milestone_index | INT | | 对应 goal.milestones_json 下标 |
| sort_order | INT | NOT NULL, DEFAULT 0 | 同日排序 |
| resources_note | VARCHAR(2000) | | AI 学习资源备注 |
| created_at / updated_at | DATETIME(3) | NOT NULL | |

---

### 3.5 `ai_invoke_log`（V2）

| 列名 | 类型 | 说明 |
|:---|:---|:---|
| log_id | BIGINT PK | |
| user_id | BIGINT, FK SET NULL | |
| goal_id | BIGINT, FK SET NULL | |
| invoke_type | VARCHAR(20) | DECOMPOSE/SCHEDULE/FEEDBACK |
| provider | VARCHAR(20) | qwen 等 |
| model | VARCHAR(30) | |
| prompt | TEXT | 脱敏或哈希 |
| prompt_tokens / completion_tokens | INT | |
| cost_cny | DECIMAL(12,4) | |
| status | VARCHAR(10) | SUCCESS/FAILED |
| response_time_ms | INT | |
| trace_id | VARCHAR(64) | 链路追踪 |
| created_at | DATETIME(3) | |

---

### 3.6 `ai_prompt`（V2）

| 列名 | 类型 | 说明 |
|:---|:---|:---|
| prompt_id | BIGINT PK | |
| type | VARCHAR(20) | DECOMPOSE/SCHEDULE/FEEDBACK |
| version | VARCHAR(20) | |
| provider | VARCHAR(20) | |
| content | TEXT | |
| is_active | TINYINT(1) | |
| is_ab_test | TINYINT(1) | |
| ab_test_bucket | VARCHAR(32) | |
| created_at / updated_at | DATETIME(3) | |

---

### 3.7 `ai_feedback`（V2）

| 列名 | 类型 | 说明 |
|:---|:---|:---|
| feedback_id | BIGINT PK | |
| user_id | BIGINT NOT NULL, FK | |
| goal_id | BIGINT, FK SET NULL | |
| feedback_type | VARCHAR(30) | EXECUTION_ANALYSIS 等 |
| summary | TEXT | |
| detail_json | JSON | |
| created_at | DATETIME(3) | |

---

### 3.8 `ai_model_config`（V2）

| 列名 | 类型 | 说明 |
|:---|:---|:---|
| config_id | BIGINT PK | |
| env | VARCHAR(20) | test/prod |
| provider | VARCHAR(20) | |
| model | VARCHAR(64) | |
| temperature | DECIMAL(4,3) | |
| max_tokens | INT | |
| is_active | TINYINT(1) | |
| created_at / updated_at | DATETIME(3) | |

---

### 3.9 `admin_user`（V3）

| 列名 | 类型 | 说明 |
|:---|:---|:---|
| admin_id | BIGINT PK | |
| username | VARCHAR(64) UK | |
| password_hash | VARCHAR(255) | |
| role | VARCHAR(32) | 默认 OPERATOR |
| enabled | TINYINT(1) | |
| created_at / updated_at | DATETIME(3) | |

---

### 3.10 `goal_decomposition_review`（V3）

| 列名 | 类型 | 说明 |
|:---|:---|:---|
| review_id | BIGINT PK | |
| goal_id | BIGINT NOT NULL, FK | |
| score | INT | 1–5 |
| issue_type | VARCHAR(40) | 见枚举 DecompositionIssueType |
| remark | VARCHAR(1000) | |
| reviewer_admin_id | BIGINT NOT NULL, FK→admin_user | |
| created_at | DATETIME(3) | |

---

### 3.11 `user_admin_tag`（V3）

| 列名 | 类型 | 说明 |
|:---|:---|:---|
| tag_id | BIGINT PK | |
| user_id | BIGINT NOT NULL, FK | |
| tag | VARCHAR(32) | 如 LIMIT_AI |
| reason | VARCHAR(500) | |
| created_by | BIGINT NOT NULL, FK→admin_user | |
| created_at | DATETIME(3) | |

---

### 3.12 `user_subscription_pref`（V3）

| 列名 | 类型 | 说明 |
|:---|:---|:---|
| pref_id | BIGINT PK | |
| user_id | BIGINT NOT NULL, UK, FK | |
| daily_reminder | TINYINT(1) | 默认 1 |
| conflict_notify | TINYINT(1) | 默认 1 |
| milestone_celebrate | TINYINT(1) | 默认 1 |
| lag_warning | TINYINT(1) | 默认 1 |
| created_at / updated_at | DATETIME(3) | |

---

## 4. JPA 实体与表名对照

| Java 类（包路径省略 `com.aigp.demo.domain`） | 表名 |
|:---|:---|
| user.AppUser | `app_user` |
| goal.Goal | `goal` |
| plan.Plan | `plan` |
| task.Task | `task` |
| ai.AiInvokeLog | `ai_invoke_log` |
| ai.AiPrompt | `ai_prompt` |
| ai.AiFeedback | `ai_feedback` |
| ai.AiModelConfig | `ai_model_config` |
| admin.AdminUser | `admin_user` |
| admin.GoalDecompositionReview | `goal_decomposition_review` |
| admin.UserAdminTag | `user_admin_tag` |
| user.UserSubscriptionPref | `user_subscription_pref` |

---

## 5. 枚举值速查（与库中 VARCHAR 一致）

### 5.1 目标状态 `goal.status`

`DECOMPOSING`，`PENDING`，`ACTIVE`，`PAUSED`，`COMPLETED`，`ABANDONED`，`NEED_DECISION`，`DECOMPOSE_FAILED`

### 5.2 计划状态 `plan.status`

`ACTIVE`，`DEPRECATED`

### 5.3 计划触发 `plan.trigger_reason`（示例）

`FIRST_SCHEDULE`，`MANUAL_REGENERATE`，`AUTO_REGENERATE`，`PROFILE_CHANGE`（以 `PlanTriggerReason` 为准）

### 5.4 任务状态 `task.status`

`PENDING`，`COMPLETED`，`SKIPPED`

### 5.5 AI 调用类型 `ai_invoke_log.invoke_type`

`DECOMPOSE`，`SCHEDULE`，`FEEDBACK`

### 5.6 AI 反馈类型 `ai_feedback.feedback_type`

`EXECUTION_ANALYSIS`，`PLAN_OPTIMIZATION`，`MILESTONE_CELEBRATION`

### 5.7 拆解评审问题 `goal_decomposition_review.issue_type`

`MISSING_MILESTONE`，`UNFAIR_DURATION`，`DEPENDENCY_ERROR`，`FORMAT_ERROR`，`OTHER`

### 5.8 成长领域 `goal.domain`（与 PRD / `GrowthDomain` 一致）

`SKILLS`，`CERTIFICATION`，`LANGUAGE`，`SOFT_SKILLS`，`SIDE_HUSTLE`

---

## 6. 接口规划（PRD）

以下摘自《需求文档》，**当前代码未实现**；实现后建议用 SpringDoc 生成 OpenAPI。

### 6.1 统一响应（PRD）

```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "timestamp": 1719820800000
}
```

- 成功 `code = 0`；错误码分段见 PRD（参数 / 业务 / 系统 / 第三方）。
- 分页响应字段约定：`content`，`totalElements`，`totalPages`，`currentPage`；请求 `page`（从 1）、`size`（默认 20，最大 100）。

### 6.2 微信小程序端（C 端）

| 方法 | 路径 | 主要读写表（规划） |
|:---|:---|:---|
| POST | `/v1/auth/login` | `app_user` |
| POST | `/v1/users/profile` | `app_user` |
| GET | `/v1/users/me` | `app_user` |
| PUT | `/v1/users/profile` | `app_user` |
| GET | `/v1/users/subscriptions` | `user_subscription_pref` |
| POST | `/v1/goals` | `goal`；异步→`ai_invoke_log`，更新 `milestones_json` |
| GET | `/v1/goals` | `goal` |
| GET | `/v1/goals/{goalId}` | `goal`，关联 `plan`/`task` |
| PUT | `/v1/goals/{goalId}/priority` | `goal` |
| PUT | `/v1/goals/{goalId}/status` | `goal` |
| GET | `/v1/tasks/today` | `task`，`goal` |
| POST | `/v1/tasks/{taskId}/complete` | `task`；异步→`ai_feedback`，`ai_invoke_log` |
| POST | `/v1/tasks/{taskId}/skip` | `task` |
| POST | `/v1/plans/feedback` | 触发展开：依赖业务设计 |
| GET | `/v1/plans/{goalId}` | `plan`，`task` |
| POST | `/v1/plans/{goalId}/regenerate` | `plan`，`task`；`ai_invoke_log` |
| GET | `/v1/stats/goal/{goalId}` | `task`，`goal` |
| GET | `/v1/stats/overview` | 聚合查询 |
| GET | `/v1/ai-feedback` | `ai_feedback` |

### 6.3 后台管理（B 端）

| 方法 | 路径 | 主要读写表（规划） |
|:---|:---|:---|
| POST | `/v1/admin/auth/login` | `admin_user` |
| GET | `/v1/admin/metrics/overview` | 聚合 / 指标 |
| GET | `/v1/admin/metrics/ai-cost` | `ai_invoke_log` |
| GET | `/v1/admin/users` | `app_user` |
| GET | `/v1/admin/users/{userId}` | `app_user` 及关联 |
| PUT | `/v1/admin/users/{userId}/tag` | `user_admin_tag` |
| GET | `/v1/admin/goals` | `goal` |
| GET | `/v1/admin/goals/{goalId}/decomposition` | `goal`，`goal_decomposition_review` |
| POST | `/v1/admin/plans/{goalId}/intervene` | `plan`，`task` |
| GET/PUT/POST | `/v1/admin/ai/prompts*` | `ai_prompt` |
| GET | `/v1/admin/ai/bad-cases` | 评审与标注相关表 |
| PUT | `/v1/admin/ai/config` | `ai_model_config` |
| GET/PUT | `/v1/admin/config/notifications` | 若落地可能扩展配置表（当前 V3 无独立通知模板表） |

---

## 7. 维护说明

- Schema 变更请新增 **V4+** 迁移脚本，勿改写已发布的 `V1–V3` 文件。
- 本文档随 Flyway 与 PRD 变更同步更新；OpenAPI 可在接入 `spring-boot-starter-web` 与 SpringDoc 后以导出 YAML 为准。
