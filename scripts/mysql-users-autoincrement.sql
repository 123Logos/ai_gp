-- =============================================================================
-- 将 C 端用户表 users.id 的自增起点设为 100000000（下一条 INSERT 且未显式指定 id 时从该值起编）
-- 在 MySQL 8 中执行一次即可；若当前表内最大 id 已 ≥ 该值，下一条自增值为 max(id)+1。
-- =============================================================================

ALTER TABLE users AUTO_INCREMENT = 100000000;
